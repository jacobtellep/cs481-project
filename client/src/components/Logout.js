import React from 'react';

const Logout = () => {
  return (
    <div
      style={{
        margin: '30px',
        width: '650px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <h1
        style={{
          margin: '30px',
          width: '650px',
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
        }}
        className="border">
        You have been logged out
      </h1>
    </div>
  );
};

export default Logout;
